const { ActionRowBuilder, ButtonBuilder, ButtonStyle } = require('discord.js');
const Discord = require("discord.js")


module.exports = {
    name: "modulelistlogsselect",
    async runInteraction(client, interaction, db, lang) {
        
    },
};